name: pull_request
on: 
   pull_request:
    branches:
        - dev
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: get the code 
              uses: actions/checkout@v4
            - name: package
              run: mvn package  
